inherit ocaml


HOMEPAGE="http://martin.jambon.free.fr/yojson.html"
SRC_URI="http://martin.jambon.free.fr/${P}.tar.bz2"

src_compile() {
	lndirs
	cd ${B}
	cygmake
}

src_install() {
	cd ${B}
	dodir /usr/bin
	cygmake install PREFIX=${D}/usr
}
